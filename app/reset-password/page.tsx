"use client";
import { FormEvent, useState } from "react";
import { ArrowRight, Command, LockKeyhole } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

export default function ResetPasswordPage(){const[password,setPassword]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const supabase=createClient();const{error}=await supabase!.auth.updateUser({password});if(error)setMessage("Não foi possível alterar a senha. Solicite um novo link.");else{setMessage("Senha alterada com sucesso.");setTimeout(()=>location.href="/dashboard",900)}setLoading(false)}return <main className="auth-simple"><div className="auth-card"><div className="auth-mobile-logo"><Command/>CENTRAL <b>LAST ONE</b></div><span className="auth-kicker">NOVO ACESSO</span><h1>Crie uma nova senha</h1><p>Use pelo menos 8 caracteres e evite senhas utilizadas em outros serviços.</p><form onSubmit={submit}><label>Nova senha<div className="auth-field"><LockKeyhole/><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mínimo de 8 caracteres"/></div></label>{message&&<div className="auth-message success">{message}</div>}<button className="auth-submit" disabled={loading}>{loading?"Salvando...":"Salvar nova senha"}<ArrowRight/></button></form></div></main>}
