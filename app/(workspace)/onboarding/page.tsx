import ModuleWorkspace from "@/components/modules/ModuleWorkspace";import{context}from"@/lib/modules/server";export default async function Page(){const{supabase,user,profile}=await context();const{data}=await supabase.from("onboarding").select("*,clients(name)").order("created_at");return <ModuleWorkspace module="onboarding" rows={data||[]} organizationId={profile.organization_id} userId={user.id}/>}
