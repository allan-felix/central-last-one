"use client";
import { FormEvent, useState } from "react";
import { ArrowLeft, ArrowRight, Command, Mail } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

export default function ForgotPasswordPage(){const[email,setEmail]=useState("");const[loading,setLoading]=useState(false);const[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const supabase=createClient();if(supabase){await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/reset-password`});}setDone(true);setLoading(false)}return <main className="auth-simple"><div className="auth-card"><div className="auth-mobile-logo"><Command/>CENTRAL <b>LAST ONE</b></div>{done?<><span className="auth-icon"><Mail/></span><h1>Verifique seu e-mail</h1><p>Se houver uma conta cadastrada, você receberá um link para redefinir sua senha.</p><a className="auth-submit" href="/login"><ArrowLeft/>Voltar ao login</a></>:<><span className="auth-kicker">RECUPERAÇÃO DE ACESSO</span><h1>Redefinir senha</h1><p>Informe seu e-mail corporativo para receber o link de recuperação.</p><form onSubmit={submit}><label>E-mail<div className="auth-field"><Mail/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@empresa.com.br"/></div></label><button className="auth-submit" disabled={loading}>{loading?"Enviando...":"Enviar link"}<ArrowRight/></button></form><a className="auth-back" href="/login"><ArrowLeft/>Voltar ao login</a></>}</div></main>}
