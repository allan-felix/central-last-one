# Integrações futuras

Cada provedor deverá viver em uma pasta própria (`google-ads`, `meta-ads`, `google-calendar`, `whatsapp`, `asaas`, `stripe`, `openai` e `n8n`). Use adapters tipados, segredos apenas no servidor, filas para sincronizações e `activity_logs` para auditoria. Nenhuma integração externa está ativa no MVP inicial.
